/**
 * Checks the health status of the application.
 *
 * @returns {Promise<boolean>} A promise that resolves to `true` if the application is healthy.
 */
export async function checkHealth(): Promise<boolean> {
  return true
}
